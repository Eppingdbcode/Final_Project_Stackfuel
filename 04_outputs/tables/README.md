# Tables

The GENESIS hero extension adds a 14,560-row additive fact, four dimensions, two summaries, lineage, controls, reconciliation and a dedicated dictionary. Official total hierarchy rows remain only in processed/control logic.

Tabelas finais verificáveis. Preserve denominadores, unidades, fontes e distinção entre resultados observados e modelados.

Run `python -m final_project_stackfuel.pipeline` from the project environment to rebuild all CSV files. Import the UTF-8 BOM CSVs into Power BI without editing them. `scenario_framework.csv` documents unavailable inputs; there is intentionally no scenario-results table.
