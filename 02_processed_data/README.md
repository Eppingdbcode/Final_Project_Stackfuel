# Processed data

Dados derivados por pipelines reproduzíveis. Registre origem, transformação, esquema, validações e limitações; nunca use esta camada para alterar RAW. Publicação depende de licença, sensibilidade e tamanho.

`raw_inventory.csv` is generated by `src/final_project_stackfuel/pipeline.py`. Final analytical tables are written to `04_outputs/tables/`; RAW remains unchanged.
